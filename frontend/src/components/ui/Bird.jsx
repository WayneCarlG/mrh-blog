import { Bird } from 'lucide-react';

const PeaceIcon = () => {
  return (
    <Bird 
      size={48} 
      color="#4a5568" // Optional: Gray/White color
      strokeWidth={1.5} 
    />
  );
};

export default PeaceIcon;